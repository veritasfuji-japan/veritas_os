name: TrustLog Primary Publication PostgreSQL

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: trustlog-primary-publication-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  primary-publication-proof:
    name: trustlog-primary-publication-postgresql
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: veritas
          POSTGRES_PASSWORD: veritas_test
          POSTGRES_DB: veritas_test
        options: >-
          --health-cmd "pg_isready -U veritas"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      PYTHONUNBUFFERED: "1"
      OPENAI_API_KEY: "DUMMY_FOR_CI"
      VERITAS_API_KEY: "DUMMY_FOR_CI"
      VERITAS_DATABASE_URL: "postgresql+psycopg://veritas:veritas_test@localhost:5432/veritas_test"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          set -euxo pipefail
          python -m pip install --upgrade pip --retries 5 --timeout 60
          python -m pip install --retries 5 --timeout 60 -r veritas_os/requirements.txt
          python -m pip install --retries 5 --timeout 60 \
            "psycopg[binary]" psycopg-pool alembic pytest pytest-asyncio

      - name: Run Alembic migrations
        run: |
          set -euxo pipefail
          alembic upgrade head

      - name: Prove primary logical uniqueness on real PostgreSQL
        run: |
          set -euxo pipefail
          mkdir -p test-reports
          pytest -q \
            veritas_os/tests/test_pg_trustlog_primary_publication.py \
            -m "postgresql and contention" \
            -v \
            --tb=short \
            --durations=10 \
            --junitxml=test-reports/trustlog-primary-publication-postgresql.xml

      - name: Run deterministic identity unit tests
        run: |
          set -euxo pipefail
          pytest -q veritas_os/tests/test_trustlog_primary_publication.py -v --tb=short

      - name: Upload proof report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trustlog-primary-publication-postgresql-${{ github.run_id }}
          path: test-reports/trustlog-primary-publication-postgresql.xml
          if-no-files-found: warn
