# Veritas OS システム総合評価（2026-03-02）

## 総合スコア

**82 / 100（A-）**

## 評価軸と点数

| 評価軸 | 点数 | 所見 |
|---|---:|---|
| アーキテクチャ整合性 | 86 | `Planner / Kernel / Fuji / MemoryOS` の責務分離が概ね守られている。 |
| 安全性・ガバナンス | 84 | Fujiゲート、監査・署名、ポリシー適用の実装が揃っている。 |
| 実装品質（保守性） | 79 | コア機能は充実している一方で、モジュール数が多く横断的理解コストが高い。 |
| テスト・検証容易性 | 80 | Frontend/Types でのテストが見える。Python側は機能の厚みに対して追加余地がある。 |
| 運用性（観測性/障害復旧） | 81 | Self-healing, reporting, audit 経路がある。運用メトリクスの標準化は今後の余地。 |
| セキュリティ堅牢性 | 82 | 署名・ハッシュ・SBOM等の土台はあるが、鍵管理/公開設定の継続監査が重要。 |

## 強み

1. **安全機構の多層化**: Fujiコード、監査ログ、署名、ポリシー群が揃っており、単一障害点を減らす設計。
2. **プロダクト面の実装成熟**: API, core pipeline, frontend console, governance UI まで一貫して構成されている。
3. **拡張余地の高い構造**: ベンチマーク、ツール、メモリエンジン、設計資料が同居し、改善サイクルを回しやすい。

## 主な改善ポイント

1. **Python側の回帰テスト拡充**: pipeline契約、異常系、policy境界のテストを増やし、障害時に「安全側に倒れる」挙動を保証する。
2. **責務境界の自動検査**: `Planner / Kernel / Fuji / MemoryOS` の境界を静的チェック（import/依存ルール）で強制し、将来的な責務逸脱を継続監視する。
3. **運用KPIの明確化**: エラー率、Fujiブロック率、自己修復成功率、証跡完全性を週次指標として標準化し、閾値逸脱時のエスカレーション経路を定義する。

## 改善ロードマップ（90日）

| 期間 | 優先テーマ | 具体アクション | 完了条件（DoD） |
|---|---|---|---|
| 0-30日 | テスト基盤の底上げ | 主要pipelineに契約テストを追加し、異常系（タイムアウト/不正入力/権限不足）を網羅する。 | 主要フローの回帰テスト成功率95%以上、失敗時に原因分類が自動出力される。 |
| 31-60日 | 境界違反の予防 | `Planner / Kernel / Fuji / MemoryOS` 間の依存ルールをCIで強制し、違反PRを自動ブロックする。 | 境界違反の新規混入ゼロ、違反時に修正ガイドがCIログに表示される。 |
| 61-90日 | 運用監査の定着 | KPIダッシュボードと週次レビューを運用し、閾値超過時の復旧手順をRunbook化する。 | KPIの欠測ゼロ、重大アラートの初動時間を基準値以内に維持する。 |

## 推奨KPI（運用標準）

| KPI | 定義 | 目標値（初期） | 監視頻度 |
|---|---|---:|---|
| Pipeline失敗率 | 全実行に占める失敗ジョブ比率 | 2%未満 | 日次 |
| Fujiブロック率 | Fujiでブロックされた要求比率 | 変動監視（急増を検知） | 日次 |
| 自己修復成功率 | self-healingが再起動なしで復旧した比率 | 70%以上 | 週次 |
| 証跡完全性 | 署名・監査ログ・ハッシュが揃った実行比率 | 99.5%以上 | 週次 |

## 実行優先順位（短期）

1. **最優先**: テスト拡充（失敗を早期検知し、誤った変更の本番流入を防ぐ）。
2. **次点**: 境界検査のCI強制（責務逸脱を予防し、保守負債を抑える）。
3. **継続施策**: KPI運用（問題の兆候を可視化し、改善サイクルを短縮する）。

## 「CODEXで改善すべきベストな改善」

**結論: `Planner / Kernel / Fuji / MemoryOS` の責務境界をCIで自動強制しつつ、境界ごとの契約テストを同時に標準化すること。**

- 理由1（再発防止）: 境界逸脱は機能追加時に静かに混入しやすく、レビューだけでは検知漏れが発生する。
- 理由2（安全性）: Fujiや監査系があっても、境界越境で検証抜けが起きると「安全側に倒れる」挙動が壊れやすい。
- 理由3（開発速度）: 依存ルール違反をPR段階で即時に落とせれば、後工程のデバッグ時間を大幅に削減できる。

### 実装方針（責務境界を越えない範囲）

1. `scripts/architecture/check_responsibility_boundaries.py` をCI必須ジョブ化する。
2. 主要pipelineについて「正常系 + 異常系（timeout/invalid input/permission）」の契約テストを追加する。
3. 境界違反時はCIログに修正ガイド（許可依存先一覧）を出力し、修正時間を短縮する。

### CODEX実行プラン（最短で効果を出す）

| 優先度 | CODEXタスク | 期待効果 | 完了条件（DoD） |
|---|---|---|---|
| P0 | `scripts/architecture/check_responsibility_boundaries.py` をPR必須ジョブに設定し、失敗時に即ブロックする。 | 責務逸脱の混入をPR時点でゼロに近づける。 | `main` への全PRで境界チェックが自動実行され、違反PRは必ずFailになる。 |
| P0 | 主要pipelineに契約テスト雛形（正常系/timeout/invalid input/permission）を追加する。 | 「安全側に倒れる」回帰を早期検知できる。 | 対象pipelineの最低4ケースがCI実行され、失敗時に原因分類を出力できる。 |
| P1 | CIログに「禁止依存」「代替実装先」「修正例リンク」を出す。 | 開発者の修正リードタイムを短縮する。 | 境界違反時に修正ガイドが1画面で確認でき、再実行までの時間が短縮される。 |
| P1 | 週次KPI（境界違反件数/契約テスト失敗率/Fujiブロック率）を定点観測する。 | 改善効果を可視化し、優先度判断を高速化する。 | 週次レビューに3指標が欠測なく記録され、閾値超過時の是正タスクが起票される。 |

### 受け入れ基準（CODEXレビュー観点）

- **境界順守**: `Planner / Kernel / Fuji / MemoryOS` を跨ぐ新規importが0件であること。
- **安全性**: 異常系契約テストでFailした場合、処理が必ず安全側（deny/fallback/logging）に倒れること。
- **運用性**: 失敗ログから「境界違反」「入力不正」「権限不足」が機械的に分類できること。
- **開発生産性**: 境界違反の修正に必要な情報がCIログ内で完結すること。

### 導入時の注意（失敗しやすい点）

1. **境界定義の曖昧さ**: 許可依存先リストが曖昧だと、CIがノイズ化して運用破綻しやすい。
2. **契約テストの粒度不足**: 正常系だけを増やしても、安全性の回帰は防げない。
3. **例外運用の放置**: 一時的なCIスキップを恒久化すると、境界強制が形骸化する。

### 90日後に目指す状態

- 境界違反PRの混入率: **実質ゼロ**。
- 主要pipelineの異常系契約テスト: **標準搭載**。
- 失敗時の調査時間: **短縮（原因分類ログで初動を高速化）**。
- セキュリティ上の逸脱（検証抜け/権限超過）: **早期検知**。

### セキュリティ警告

- **警告**: 境界検査を導入しても、秘密情報の取り扱い（APIキー、署名鍵、内部URL）に対する運用監査が弱いと、攻撃面は残る。
- **警告**: LLM入力経路では prompt/data injection が継続リスクであり、入力検証・サニタイズ・最小権限を同時に強化する必要がある。

## セキュリティ警告（重要）

- **警告**: ガバナンスや署名基盤があっても、実運用での鍵ローテーション・秘密情報管理・公開設定監査が崩れると、信頼性は急速に低下する。
- **警告**: LLM連携やWeb連携を含むため、入力汚染（prompt/data injection）に対する継続的防御（検証・サニタイズ・最小権限）が必須。

## 現時点の評価結論

- 現状は **「研究～実運用の橋渡し段階として高水準」**。
- ただし、今後のスケールを見据えるなら **テスト密度・境界強制・運用監査の自動化** を優先すべき。
