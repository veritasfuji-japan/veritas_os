# =============================
# VERITAS OS — Dependencies
# =============================
# All versions are pinned for reproducibility and security.
#
# This file installs the FULL dependency set (equivalent to `pip install ".[full]"`).
# For lighter installs see pyproject.toml [project.optional-dependencies].
#
# Install profiles:
#   pip install -r veritas_os/requirements.txt   # full (CI / Docker default)
#   pip install .                                 # core-only
#   pip install ".[ml]"                           # + ML tooling
#   pip install ".[reports]"                      # + reporting / PDF
#   pip install ".[anthropic]"                    # + Anthropic provider
#   pip install ".[dev]"                          # + pytest / test tooling
#   pip install ".[full]"                         # everything

# --- Core: API / Server ---
fastapi==0.121.0
uvicorn==0.30.3
pydantic==2.8.2
python-dotenv==1.0.1
orjson==3.11.6
PyYAML==6.0.1
jinja2==3.1.6

# --- Core: LLM Integration ---
openai==1.51.0
httpx==0.27.2

# --- Core: Numerical ---
numpy==1.26.4

# --- Optional [ml]: Model / Decision Engine ---
scikit-learn==1.5.2
sentence-transformers==5.3.0
transformers==5.5.0

# --- Optional [reports]: CLI / Dashboard / PDF Tools ---
matplotlib==3.9.2
pandas==2.2.3
pdfplumber==0.11.9
pdfminer.six==20251230

# --- Optional [anthropic]: Anthropic LLM provider ---
anthropic==0.34.2

# --- Optional [system]: System Tools ---
psutil==6.0.0
trio==0.26.2
starlette==0.49.1

# --- Optional [observability]: Metrics / Telemetry ---
opentelemetry-api==1.28.2
opentelemetry-sdk==1.28.2
opentelemetry-exporter-otlp==1.28.2
prometheus-client==0.20.0

# --- Optional [signing]: Policy Bundle Signing ---
cryptography==46.0.7

# --- Optional [postgresql]: PostgreSQL Backend ---
psycopg[binary]==3.2.9
psycopg-pool==3.2.6
alembic==1.18.4

# --- Optional [dev]: Test / QA Tooling ---
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.0.0
